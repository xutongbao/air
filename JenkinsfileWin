pipeline {
    agent any
    environment { 
        CI = 'true'
    }
    stages {
        stage('install') {         
            steps {
                bat "${branch}"
                bat 'yarn -v'
                // bat "xcopy D:\\Jenkins\\workspace\\air D:\\temp\\air\\${PORT}\\ /Y /E"
                // bat "cd temp/air/${PORT}"
                // bat "dir"
                // bat "yarn"
                // bat "set PORT=${PORT} && node app"
            }
        }
    }
}