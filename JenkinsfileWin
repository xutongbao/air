pipeline {
    agent any
    environment { 
        CI = 'true'
    }
    stages {
        stage('install') {         
            steps {
                bat "echo ${branch}"
                bat 'yarn -v'
                bat "xcopy D:\\Jenkins\\workspace\\air D:\\temp\\air\\${branch}\\ /Y /E"
                // bat "cd temp/air/${PORT}"
                // bat "dir"
                // bat "yarn"
                // bat "set PORT=${PORT} && node app"
            }
        }
    }
}